export function Worklog() {
  return (
    <div className="panel prose">
      <h2>AI Worklog</h2>

      <h3>Инструменты</h3>
      <ul>
        <li>
          <strong>Claude Code (модель Sonnet 5)</strong> в десктоп-приложении Claude — генерация кода,
          тестов, README и этого worklog, запуск сборки и тестов, публикация живой версии как
          Claude Artifact.
        </li>
        <li>Локально: Node 25, Vite 5, Vitest 2, TypeScript 5, React 18.</li>
        <li>Git</li>
      </ul>

      <h3>Время и токены</h3>
      <ul>
        <li>
          <strong>Время разработки:</strong> около 2 часов в одной сессии.
        </li>
        <li>
          <strong>Токены:</strong> Тратил лимит по подписке (обновляется раз в 5 часов)
        </li>
      </ul>

      <h3>Этапы</h3>

      <div className="stage">
        <h4>1. Идея и уточнение ТЗ</h4>
        <p>
          <strong>Я:</strong> выбрал стек (Vite + React + TS), решил, что рабочая ссылка будет
          собираться в один HTML, отложил деплой и GitHub.
        </p>
        <p>
          <strong>AI:</strong> задал уточняющие вопросы по деплою и стеку, разложил требования ТЗ на
          проверяемые пункты (статусы, риск-логика, невозможные состояния, журнал, сводка, обязательные
          состояния).
        </p>
        <blockquote>
          «Сделай веб-приложение для смотрителя лаборатории порталов: список, карточка, расчёт риска по
          объяснимой формуле, минимум 3 действия, запрет нелогичных действий, журнал, сводка,
          обязательные состояния, встроенный AI Worklog».
        </blockquote>
      </div>

      <div className="stage">
        <h4>2. Архитектура</h4>
        <p>
          <strong>Я:</strong> потребовал разделить домен и UI — вся логика чистыми функциями без React,
          чтобы её можно было покрыть юнит-тестами. Единый источник правды для запретов.
        </p>
        <p>
          <strong>AI:</strong> предложил структуру <code>src/domain</code> (types, risk, actions,
          reducer, summary, seed) + <code>src/ui</code>, редьюсер как чистую функцию, React только как
          тонкий слой (<code>useReducer</code> + контекст).
        </p>
        <blockquote>
          «Вынеси риск-расчёт, гарды действий и редьюсер в чистый модуль без зависимости от React;
          React — только рендер и контекст».
        </blockquote>
      </div>

      <div className="stage">
        <h4>3. Риск-логика</h4>
        <p>
          <strong>Я:</strong> задал смысл формулы (низкая стабильность + высокая энергия + мало времени
          = высокий риск), настоял на объяснимости — таблица слагаемых прямо в карточке.
        </p>
        <p>
          <strong>AI:</strong> предложил взвешенную сумму 0..100 (веса 0.45 / 0.30 / 0.15 / 0.10),
          пороги уровней 25/50/75 и два жёстких override.
        </p>
        <blockquote>
          «Формула риска: взвешенная сумма нормализованных 0..100 факторов, веса в сумме = 1, плюс
          hard-override: ≤10 мин → не ниже high; стабильность&lt;15 и энергия&gt;80 → critical. Верни
          разбор слагаемых для UI».
        </blockquote>
      </div>

      <div className="stage">
        <h4>4. Действия порталов и невозможные состояния</h4>
        <p>
          <strong>Я:</strong> решил, что запрещённую кнопку не прячем — по ней можно нажать, и отказ
          попадает в журнал (иначе обязательный сценарий «попытка запрещённого действия» нечем
          показать). Разделил запреты на жёсткие и требующие подтверждения.
        </p>
        <p>
          <strong>AI:</strong> реализовал <code>canRun()</code> с типом результата «можно / жёстко
          нельзя / нужно подтверждение», применил его и в UI (вид кнопки), и в редьюсере (защита
          состояния).
        </p>
        <blockquote>
          «Гард действий: закрытый портал нельзя стабилизировать / закрывать повторно / метить;
          наблюдатель запрещён при critical и требует подтверждения при high; закрытие с существами —
          через подтверждение. Тот же гард вызывает редьюсер».
        </blockquote>
      </div>

      <div className="stage">
        <h4>5. UI</h4>
        <p>
          <strong>Я:</strong> выбрал раскладку (сводка сверху, список + карточка в две колонки, журнал
          снизу), тёмную «консоль лаборатории», вкладки Панель / Worklog / Чеклист.
        </p>
        <p>
          <strong>AI:</strong> сверстал компоненты, счётчики сводки, метрики-полоски, тосты и модалку
          подтверждения; типографику (IBM Plex Sans/Mono, моноширинный для чисел и меток).
        </p>
        <blockquote>
          «Тёмная приборная консоль: сводка сверху, слева таблица порталов, справа карточка, журнал
          снизу. Вкладки Панель / Worklog / Чеклист. Числа и метки — моноширинным, риск-бейджи цветом».
        </blockquote>
      </div>

      <div className="stage">
        <h4>6. Тесты и отладка</h4>
        <p>
          <strong>Я:</strong> определил список обязательных сценариев, проверил их руками в браузере.
        </p>
        <p>
          <strong>AI:</strong> написал 44 юнит-теста на домен (risk / actions / reducer / summary / text / seed),
          починил конфиг (<code>tsc -b</code> без <code>tsconfig.node.json</code> → просто{' '}
          <code>tsc</code>; типизация vitest через тройной слэш-референс), прогнал сборку.
        </p>
        <blockquote>«Покрой тестами формулу, override, каждый запрет, изменение риска после стабилизации, пустой список и порядок журнала».</blockquote>
      </div>

      <div className="stage">
        <h4>7. Финальная проверка и упаковка</h4>
        <p>
          <strong>Я:</strong> прошёл Definition of Done глазами «другого человека»: открыл, понял
          сценарий, выполнил действия, поискал сломанные пути.
        </p>
        <p>
          <strong>AI:</strong> собрал единый <code>dist/index.html</code>, подготовил архив с
          лаунчером, README и чеклист.
        </p>
        <blockquote>
          «Собери один самодостаточный index.html (vite-plugin-singlefile), архив с .bat-лаунчером,
          README с быстрым стартом и вкладку-чеклист со сценариями проверки».
        </blockquote>
      </div>

      <div className="stage">
        <h4>8. Доводка по правкам</h4>
        <p>
          <strong>Я:</strong> попросил сделать раскладку перекраиваемой мышью (полосы между колонками
          и панелями, размеры чтоб запоминались) и добавить кнопку «случайный портал».
        </p>
        <p>
          <strong>AI:</strong> сделал сплиттеры (CSS Grid для колонок + хук <code>useDragValue</code> с
          сохранением в <code>localStorage</code>), одну общую полосу на границу колонок, генератор{' '}
          <code>randomPortal()</code> с подменяемым ГПСЧ под тесты.
        </p>
        <blockquote>
          «Панели резайзятся за рамки: одна полоса между колонками — ширина, одна между карточкой и
          историей — деление высоты, нижний край — общая высота (список тянется следом). Размеры в
          localStorage, двойной клик — сброс».
        </blockquote>
      </div>

      <h3>3–5 решений, которые я принял сам</h3>
      <ol>
        <li>Домен отдельно от UI: риск, гарды и редьюсер — чистые функции, поэтому их реально протестировать.</li>
        <li>Запрещённое действие не прячем, а логируем отказ — иначе обязательный сценарий не виден в журнале.</li>
        <li>Риск — объяснимая взвешенная формула + явные override, а не «магическая» эвристика.</li>
        <li>
          Один <code>canRun()</code> как единственный источник правды о запретах — его зовёт и кнопка,
          и редьюсер.
        </li>
        <li>Сборка в один HTML-файл, чтобы «рабочая ссылка» = двойной клик без сервера.</li>
      </ol>

      <h3>Где AI ошибся или предложил слабое решение</h3>
      <ul>
        <li>
          Сначала сгенерировал <code>build: "tsc -b"</code> с project references, но без{' '}
          <code>tsconfig.node.json</code> — сборка бы упала. Упростил до <code>tsc &amp;&amp; vite build</code>.
        </li>
        <li>
          Первая версия формулы была чисто линейной и недооценивала «10 минут до схлопывания» при
          неплохой стабильности. Добавили жёсткие override.
        </li>
        <li>
          Предлагал делать запрещённые кнопки <code>disabled</code> — тогда попытку запрещённого
          действия невозможно показать в журнале. Переиграли на «нажатие → отказ в лог».
        </li>
        <li>Изначально не типизировал секцию <code>test</code> в конфиге Vite — падал <code>tsc</code>.</li>
        <li>
          Для «список = высоте правой колонки» предложил <code>ResizeObserver</code> — он не срабатывал
          в фоновой вкладке. Заменили на чистый CSS (<code>align-items: stretch</code>).
        </li>
        <li>
          Резайз ширины колонок через flexbox давал горизонтальную прокрутку при перетаскивании —
          переехали на CSS Grid.
        </li>
      </ul>

      <h3>Что переписано / доработано вручную</h3>
      <ul>
        <li>Веса и пороги формулы риска подобраны так, чтобы демо-порталы попадали в нужные уровни.</li>
        <li>Тексты рекомендаций и причин запретов — переписаны под смотрителя, не «программистские».</li>
        <li>Сценарные кнопки (пустой список, +критический портал, +случайный портал, сброс) — для быстрой проверки.</li>
        <li>Раскладка и визуальные акценты (критическая карточка красная, метрики-полоски).</li>
        <li>
          Модель резайза переигрывалась несколько раз по правкам: от нативного CSS <code>resize</code>{' '}
          к сплиттерам с одной общей полосой на каждую границу.
        </li>
      </ul>

      <h3>Как проверял, что приложение работает</h3>
      <ul>
        <li>
          <code>npm test</code> — 44 юнит-теста на домен (формула, override, все запреты, изменение
          риска после стабилизации, пустой список, порядок журнала).
        </li>
        <li><code>npm run build</code> — сборка проходит, типы чистые.</li>
        <li>Ручной прогон в браузере всех сценариев из вкладки «Чеклист».</li>
        <li>
          Резайз-полосы — замерами в DevTools: границы двигаются, список синхронён с высотой колонки,
          горизонтальной прокрутки нет, размеры переживают перезагрузку.
        </li>
        <li>Проверка Definition of Done глазами нового человека: понятно ли за минуту, нет ли тупиков.</li>
      </ul>

      <h3>Что бы доработал в реальном продукте</h3>
      <ul>
        <li>Персистентность: состояние (порталы, журнал) в памяти, при перезагрузке сбрасывается — сохраняются только размеры панелей.</li>
        <li>Роли и аудит: кто именно выполнил действие, подпись смены.</li>
        <li>Тесты на компоненты, e2e и на резайз-логику — сейчас покрыт только домен.</li>
        <li>Резайз-полосы только мышью, без клавиатуры.</li>
        <li>Живой обратный отсчёт времени до схлопывания со звуковой тревогой на критическом.</li>
        <li>Конфигурируемые пороги и веса риска без пересборки.</li>
      </ul>
    </div>
  );
}
